# Travel Agent Itinerary App

A small React + TypeScript app for displaying a travel itinerary with dates and activities.

## Run locally

1. Install dependencies:

```bash
npm install
```

2. Start the development server:

```bash
npm run dev
```

3. Open the URL shown in the terminal, typically `http://localhost:5173`.
